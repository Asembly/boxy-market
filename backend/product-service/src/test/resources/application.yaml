spring:
  application:
    name: product-service
server:
  port: 8083
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic
  service:
    storage-service: http://localhost:8108/storage-service
    user-service: http://localhost:8081/user-service
